export const CONTENT_TYPES = {
  ARTICLES: 'articles',
  HTML: 'html',
  CONTACTS: 'contacts',
  CALENDAR: 'calendar',
};

export const CONTENT_TYPES_ARRAY = Object.keys(CONTENT_TYPES).map(key => CONTENT_TYPES[key]);
